const name = 'Deadpool';
const real = 'Wade Winston';

const normal = name + ' ' + real;
const template = `${name} ${real}`;

console.log(normal);
console.log(template);

const html = `
  <h1>Hello</h1>
  <p>World!<p>
`;
console.log(html);
